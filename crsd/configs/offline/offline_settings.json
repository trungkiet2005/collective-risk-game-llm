{
  "backend": "vllm",
  "modelPreset": "models_qwen2.5-7b",
  "_comment_seed": "Seed sinh văn bản KHÔNG cố định ở đây nữa. Runner suy ra seed per-lượt = exp.seed + rep + round + agent (xem CrsdGame.sampling_seed) -> vừa reproducible vừa độc lập giữa các rep. 'seedBase' chỉ là offset toàn cục tuỳ chọn (mặc định 0).",
  "sampling": {
    "temperature": 0.7,
    "maxTokens": 512,
    "seedBase": 0
  },
  "engine": {
    "maxModelLen": 4096,
    "gpuMemoryUtilization": 0.90,
    "tensorParallelSize": 1,
    "dtype": "bfloat16",
    "trustRemoteCode": true
  },
  "batchSize": 0
}
