%% =====================================================================================
%% P-7 — Discussion, limitations, ethics.  Muc LaTeX DOC LAP.
%%
%% BA VIEC, DUNG THU TU: (1) ba doi chung cong lai NOI GI, (2) no doi gi o nguoi trien
%% khai, (3) gioi han — KHAI THANG, khong giau xuong phu luc.
%%
%% ⛔ KHONG DUOC BO DOAN "monoculture" TRONG LIMITATIONS. §4 va §5 van la self-play, va do
%% dung la dieu dau tien reviewer AAMAS hoi. Chung ta tra loi bang §6 (doi thu scripted)
%% chu KHONG bang quan the hon hop that — noi thang ra, dung de reviewer tu phat hien.
%%
%% ⛔ KHONG DUOC HUA TAI LAP THEO SEED. Do duoc: chay lai cung o voi cung seed cho ket qua
%% khac o 57% so o. Chi hua tai lap ve THIET KE. Xem CLAUDE.md.
%%
%% KHONG CO "Conclusion" RIENG. Mot ket luan viet lai intro o thi qua khu la muc bi cat
%% dau tien trong moi bai 8 trang; doan cuoi cua muc nay lam dung viec do trong ba cau.
%%
%% NGAN SACH TRANG: 1,2 cot / 16 cot. Khong float.
%% =====================================================================================

\section{Discussion}
\label{sec:discussion}


An evaluation that seats one model against fixed opponents cannot detect the
behaviour a deployment depends on. Fixed opponents give nothing to respond to, and
one model in every seat gives nothing to tell apart, so a schedule and a policy
leave the same transcript. Section~\ref{sec:mixed} is built to separate them, and
finds that almost none of the responding is there.

Five diagnostic layers converge on one qualified description. Risk, prompt and comprehension
controls show that the contribution schedule is largely fixed and available arithmetic is
unused. Scripted opponents show no best response in the strictly dominated games, while
mixed populations show that apparent partner response largely reflects the running pool.
E6 shows that this pattern survives two prompt paraphrases and temperature-zero decoding
for four models, while Grok retains a risk response. What these agents bring to a
collective-risk setting is therefore usually a fixed contribution schedule, close to the
focal split, rather than a uniformly risk-insensitive policy.

For a system designer the consequence is asymmetric, and it is the reason we report a gap
to the optimum rather than a success rate. As collaborators these agents are excellent:
they reach the group target in most populations, they do not defect opportunistically, and
they never hold the group hostage. As stakeholders they are unsafe, because they will not
protect the resources entrusted to them. A scripted free-rider out-earns every model in
the panel (\S\ref{sec:bestresponse}), and a group-level metric records that outcome as a
success. The two failure modes we observe are invisible to such a metric from opposite
sides: Grok reaches the target in
$\PanelReachGrok\%$ of games while finishing with $\PanelPayGrok$ of
$\PanelEndowment$ units, and Qwen pays $\PanelOwnQwen$ units into pots
that never reach the target at all. A deployment that reads only whether the shared goal
was met will rank both as it should not.

\paragraph{Limitations.}
Sections~\ref{sec:riskgrid} and~\ref{sec:controls} are self-play: all \PanelNplayersWord{} seats hold one
model. That design is the object of our criticism. We complement it with fixed opponents
in Section~\ref{sec:bestresponse}, where the best response is enumerable, and with the
balanced round-robin in Section~\ref{sec:mixed}, where a second model can respond. Neither
is a naturalistic deployment: scripted opponents buy exactness at the cost of realism, and
the mixed tables contain only two model types at a time, without communication or
institutional rules. The panel is \PanelNmodelsWord{} models, one per provider and all at the inexpensive tier, so a
capability effect within a family is outside what we can see; cells hold
$\PanelNpercell$ games, which is why every interval is a bootstrap and every test a
permutation test on games rather than on seats. We run one prompt template at one
temperature in the main panel, although E6 adds two endpoint paraphrases and a
temperature-zero arm. E6 is still not a systematic sensitivity analysis over prompt
families, temperatures, or model capabilities. Pricing a gap in
expected payoff assumes risk neutrality; we therefore rest the conclusions on the $p=0$
column and on the strictly dominated profiles, where contributing loses under every
increasing utility and no such assumption is in play. One of the two value probes asks for
a point estimate mid-game and is ambiguous between ``every round'' and ``every remaining
round''. Several wrong answers are correct readings of the second, so we report its
$\PanelProbeEvPointAcc\%$ for completeness and rest nothing on it. Finally, the game-level
randomisation is seeded and reproduces, but the model text does not: replaying identical
cells at identical seed and temperature returns different play. We release prompts,
configurations, seeds and code, and promise reproducibility of the design rather than of
the transcripts.

\paragraph{Ethics.}
The study runs no human subjects and collects no personal data. Its result is a negative
one about deployed systems, and we report it in the direction that is useful: agents that
cooperate unconditionally are safe to place in a group and unsafe to place in charge of
something, and the distance between those two sentences is what a measurement against the
best response makes visible.
